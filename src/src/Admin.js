import React from "react";
import { render } from "react-dom";
import Title from "./Title";

function Admin() {
    return(
        Title("Admin", "Admin page for management")
    );
}

export default Admin;